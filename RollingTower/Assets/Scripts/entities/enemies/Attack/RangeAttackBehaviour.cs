public class RangeAttackBehaviour : AbstractUnitAttack {

    private TargetedProjectile _projectile;
    
    public override void Attack(IDamageable target) {
        //_projectile.Init(target, 1, 0.1f);
    }
}